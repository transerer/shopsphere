server:
  port: 8080

spring:
  application:
    name: gateway

  cloud:
    gateway:
      routes:
        - id: user-service
          uri: http://localhost:8081
          predicates:
            - Path=/users/**
        - id: auth-service
          uri: http://localhost:8082
          predicates:
            - Path=/auth/**
#          filters:
#            - StripPrefix=1